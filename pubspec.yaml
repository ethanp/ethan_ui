name: ethan_ui
description: Shared machined-console UI theme and chrome for Ethan's Flutter apps.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.13.0
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  ethan_utils:
    path: ../ethan_utils
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
